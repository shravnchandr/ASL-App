# ASL Dictionary API - Python Dependencies

# Direct Gemini API (replaces LangChain/LangGraph to reduce ~120MB baseline RAM)
# Original LangChain implementation preserved in python_code/asl/nodes.py for reference.
google-genai>=1.0.0
pydantic>=2.0.0

# Existing dependencies
colorama>=0.4.6
python-dotenv>=1.0.0

# FastAPI and Server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9

# Rate Limiting
slowapi>=0.1.9

# Logging
loguru>=0.7.2

# Database (Async SQLite; asyncpg available if switching back to PostgreSQL)
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.20.0

# Semantic similarity (optional — disabled by default, requires ENABLE_SEMANTIC_LOOKUP=true)
# Uncomment if enabling on an instance with >700MB RAM available:
# sentence-transformers>=3.0.0
# numpy>=1.26.0

# Caching
redis[asyncio]>=5.0.0

# Settings Management
pydantic-settings>=2.0.0

# Testing (optional)
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
httpx>=0.27.0  # For testing FastAPI endpoints
